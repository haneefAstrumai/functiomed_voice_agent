FROM node:20-slim

WORKDIR /app

# context = ./frontend
# so voice_agent_frontend/ resolves to ./frontend/voice_agent_frontend/
COPY voice_agent_frontend/package*.json ./

RUN npm install

COPY voice_agent_frontend/ .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]